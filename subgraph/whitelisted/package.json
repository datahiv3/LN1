{
  "name": "whitelisted",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:studio": "graph deploy --studio ln1 --network=optimism-sepolia",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ ln1/whitelisted",
    "create-local": "graph create --node http://localhost:8020/ --network=localhost ln1/whitelisted",
    "remove-local": "graph remove --node http://localhost:8020/ --network=localhost ln1/whitelisted",
    "deploy-local": "graph deploy --node http://localhost:8020/ --network=localhost --ipfs http://localhost:5001 ln1/whitelisted",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.73.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
